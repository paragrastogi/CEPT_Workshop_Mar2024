# Declare default colours for the code which calls this script.
# import numpy as np
from numpy import array

# Deep blue.
blue = array((25, 100, 200)) / 255
# Pure f***ing blue.
bluest = array((0, 0, 255)) / 255

# Distinguished looking grey.
grey = array((0.3, 0.3, 0.3))
# Also distinguished but contrasts better with black.
lgrey = array((0.6, 0.6, 0.6))

# Indian flag orange (saffron).
orange = array((255, 128, 0)) / 255
# Orange like nice satsumas.
orangest = array((255, 165, 0)) / 255

# Oxygenated blood red.
red = array((160, 30, 30)) / 255
# Pure f***ing red.
reddest = array((255, 0, 0)) / 255

# Gandalf white.
whitest = array((1, 1, 1))
# Black as coal, black as night.
blackest = array((0, 0, 0))

# Veering towards gold.
yellow = array((237, 177, 32)) / 255

# Gentle dark green.
green = array((40, 180, 20)) / 255
# Pure f***ing green.
greenest = array((0, 255, 0)) / 255

# Hot pink
hotpink = array((255, 29, 142)) / 255

# arbnco colours

# general
teal_pantone7712 = array((0, 133, 155)) / 255
teal_pantone7710 = array((77, 194, 203)) / 255

# climate
azure = array((0, 127, 255)) / 255

# renew
olive = array((140, 198, 63)) / 255

# energy
a_dark_green = array((51, 149, 88)) / 255

# well
a_purple = array((108, 32, 123)) / 255

# estates
a_orange = array((233, 88, 12)) / 255

# consult
lblue = array((91, 194, 231)) / 255
